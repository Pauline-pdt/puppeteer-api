services:
  - type: web
    name: puppeteer-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    nodeVersion: 18.20.0
    envVars:
      - key: NODE_ENV
        value: production
